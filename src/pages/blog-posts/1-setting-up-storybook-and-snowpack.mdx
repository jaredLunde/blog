export const metadata = {
  title: 'Setting up Storybook in a Snowpack project',
  description: (
    <React.Fragment>
      In this post I cover how to make Storybook play nicely with Snowpack
      semantics like <code>import.meta.env</code> instead of{' '}
      <code>process.env</code>
    </React.Fragment>
  ),
  timestamp: 1597879288280,
  tags: ['storybook', 'snowpack'],
  categories: ['Frontend'],
}
